apiVersion: v1
kind: Service
metadata:
  name: {{ include "trainapp.fullname" . }}
  labels:
    {{- include "trainapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "trainapp.selectorLabels" . | nindent 4 }}
